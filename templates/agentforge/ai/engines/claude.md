# Claude Notes

- Use `agentforge` ou `/agentforge`, conforme o ambiente.
- Respeite `CLAUDE.md` como ponto de entrada.
- Leia o playbook da fase e siga o handoff gerado.
- Registre `agentforge checkpoint <phase> --status done` ao concluir.
